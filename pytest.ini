[pytest]
pythonpath = .
testpaths = tests
addopts = -p no:cacheprovider
markers =
    unit: unit tests, no database required
    integration: integration tests using a disposable PostgreSQL database
    live_db: tests against a live/staging database, requires explicit flag